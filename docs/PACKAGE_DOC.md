# Package documentation

The documentation for the package itself — what the types are for, what the
`timezone` argument of `Render` and `RenderWithFormat` accepts, and how zones
are cached — lives in the package comment in [`doc.go`](../doc.go).

Read it there, or rendered at
[pkg.go.dev/github.com/aki-kuramoto/wantai](https://pkg.go.dev/github.com/aki-kuramoto/wantai).

It is not repeated here: one copy is one thing to keep true.
